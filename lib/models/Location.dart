class LocationData{

  int id;
  String name;
  String description;
  DateTime date;
  double longitude;
  double latitude;
  int countryId;
  int imageId;


  LocationData(this.id, this.name, this.description, this.date, this.longitude, this.latitude, this.countryId, this.imageId);
}