import 'package:flutter/material.dart';

class SafeButton extends StatelessWidget {
  const SafeButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
